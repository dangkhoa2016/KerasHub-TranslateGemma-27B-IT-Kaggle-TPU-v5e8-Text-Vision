name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cpu-static-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install CPU-only test dependencies
        run: python -m pip install -r requirements-ci.txt
      - name: Unit tests
        run: bash scripts/test_unit.sh
      - name: Documentation parity and public-history hygiene
        run: python scripts/check_docs.py
      - name: Notebook JSON
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          for path in Path("notebooks").glob("*.ipynb"):
              json.loads(path.read_text(encoding="utf-8"))
              print(f"VALID JSON: {path}")
          PY
      - name: Python syntax
        run: python -m compileall -q src scripts tests
      - name: Bash syntax
        run: find scripts -type f -name '*.sh' -print0 | xargs -0 -n1 bash -n
      - name: Node client syntax
        run: node --check clients/node/translategemma-client.mjs
      - name: Secret scan
        run: python scripts/secret_scan.py .
      - name: Build and verify clean source archive
        run: |
          python scripts/package_source.py /tmp/KerasHub-TranslateGemma-27B-IT-Kaggle-TPU-v5e8-Text-Vision-v1.0.0.zip
          python scripts/secret_scan.py /tmp/KerasHub-TranslateGemma-27B-IT-Kaggle-TPU-v5e8-Text-Vision-v1.0.0.zip
          unzip -t /tmp/KerasHub-TranslateGemma-27B-IT-Kaggle-TPU-v5e8-Text-Vision-v1.0.0.zip
